[project]
name = "todoist"
version = "0.1.0"
description = "A fastapi application for managing todos"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "python-dotenv>=1.1.0",
    "slowapi>=0.1.9",
    "sqlalchemy>=2.0.41",
]
